# Home for the .zarr files

Holds historical data in the form of .zarr files. The .zarr files are created from the ROSbag files in the `dataset` folder. The .zarr files are used for training the models.
